import { PaymentsCustomerService } from './payments.customer.service';

describe('PaymentsCustomerService', () => {
  it('should be defined', () => {
    expect(new PaymentsCustomerService()).toBeDefined();
  });
});
